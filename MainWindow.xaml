<Window x:Class="TypingPractice.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="青少年打字练习" Height="500" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#f5f5f5">
    
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="青少年打字练习" 
                   FontSize="36" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,40"/>
        
        <!-- 选项 -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <!-- 模式选择 -->
            <TextBlock Text="选择模式：" FontSize="18" Margin="0,0,0,15"/>
            
            <Button Content="🇨🇳 拼音练习" Click="OnPinyinMode"
                    FontSize="20" Height="50" Margin="0,0,0,15"
                    Background="#4CAF50" Foreground="White"/>
            
            <Button Content="🇬🇧 英语练习" Click="OnEnglishMode"
                    FontSize="20" Height="50" Margin="0,0,0,15"
                    Background="#2196F3" Foreground="White"/>
            
            <Button Content="📜 古诗词练习" Click="OnPoetryMode"
                    FontSize="20" Height="50" Margin="0,0,0,25"
                    Background="#9C27B0" Foreground="White"/>
            
            <!-- 难度选择 -->
            <TextBlock Text="选择难度：" FontSize="18" Margin="0,0,0,15"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="简单" Click="OnEasy" Width="80" Height="35" Margin="5,0"
                        x:Name="EasyBtn" Background="#ddd"/>
                <Button Content="中等" Click="OnMedium" Width="80" Height="35" Margin="5,0"
                        x:Name="MediumBtn" Background="#4CAF50" Foreground="White"/>
                <Button Content="困难" Click="OnHard" Width="80" Height="35" Margin="5,0"
                        x:Name="HardBtn" Background="#ddd"/>
            </StackPanel>
        </StackPanel>
        
        <!-- 底部 -->
        <TextBlock Grid.Row="2" Text="版本 1.4.0" 
                   FontSize="12" Foreground="Gray"
                   HorizontalAlignment="Center"/>
    </Grid>
</Window>
