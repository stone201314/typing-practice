<Window x:Class="TypingPractice.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="青少年打字练习" Height="500" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#f5f5f5">
    
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="青少年打字练习" 
                   FontSize="36" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,40"/>
        
        <!-- 选项 -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock Text="选择练习类型：" FontSize="18" Margin="0,0,0,20"/>
            
            <Button Content="🇨🇳 中文练习" Click="OnChineseMode"
                    FontSize="24" Height="60" Margin="0,0,0,20"
                    Background="#4CAF50" Foreground="White"/>
            
            <Button Content="🇬🇧 英文练习" Click="OnEnglishMode"
                    FontSize="24" Height="60" Margin="0,0,0,30"
                    Background="#2196F3" Foreground="White"/>
            
            <!-- 说明 -->
            <Border Background="#fff" CornerRadius="8" Padding="15" Margin="0,10,0,0">
                <StackPanel>
                    <TextBlock Text="练习内容：" FontWeight="Bold" Margin="0,0,0,8"/>
                    <TextBlock Text="• 中文：汉字→拼音（词组、古诗词）" FontSize="13" Margin="0,0,0,5"/>
                    <TextBlock Text="• 英文：中文→英文（单词、句子）" FontSize="13"/>
                </StackPanel>
            </Border>
        </StackPanel>
        
        <!-- 底部 -->
        <TextBlock Grid.Row="2" Text="版本 1.6.0" 
                   FontSize="12" Foreground="Gray"
                   HorizontalAlignment="Center"/>
    </Grid>
</Window>
