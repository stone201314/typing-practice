<Window x:Class="TypingPractice.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="青少年打字练习" Height="550" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#f5f5f5">
    
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="青少年打字练习" 
                   FontSize="36" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,30"/>
        
        <!-- 选项 -->
        <StackPanel Grid.Row="1" VerticalAlignment="Top">
            <!-- 模式选择 -->
            <TextBlock Text="选择模式：" FontSize="18" Margin="0,0,0,10"/>
            
            <Button Content="🇨🇳 拼音练习" Click="OnPinyinMode"
                    FontSize="20" Height="50" Margin="0,0,0,10"
                    Background="#4CAF50" Foreground="White"/>
            
            <Button Content="🇬🇧 英语练习" Click="OnEnglishMode"
                    FontSize="20" Height="50" Margin="0,0,0,10"
                    Background="#2196F3" Foreground="White"/>
            
            <Button Content="📜 古诗词练习" Click="OnPoetryMode"
                    FontSize="20" Height="50" Margin="0,0,0,25"
                    Background="#9C27B0" Foreground="White"/>
            
            <!-- 难度选择 -->
            <TextBlock Text="选择难度：" FontSize="18" Margin="0,0,0,10"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Button Content="简单(单字)" Click="OnEasy" Width="100" Height="35" Margin="5,0"
                        x:Name="EasyBtn" Background="#ddd"/>
                <Button Content="中等(词组)" Click="OnMedium" Width="100" Height="35" Margin="5,0"
                        x:Name="MediumBtn" Background="#4CAF50" Foreground="White"/>
                <Button Content="困难(句子)" Click="OnHard" Width="100" Height="35" Margin="5,0"
                        x:Name="HardBtn" Background="#ddd"/>
            </StackPanel>
            
            <!-- 数量选择 -->
            <TextBlock Text="练习数量：" FontSize="18" Margin="0,0,0,10"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Button Content="5个" Click="OnCount5" Width="60" Height="35" Margin="5,0"
                        x:Name="Count5Btn" Background="#ddd"/>
                <Button Content="10个" Click="OnCount10" Width="60" Height="35" Margin="5,0"
                        x:Name="Count10Btn" Background="#4CAF50" Foreground="White"/>
                <Button Content="20个" Click="OnCount20" Width="60" Height="35" Margin="5,0"
                        x:Name="Count20Btn" Background="#ddd"/>
                <Button Content="50个" Click="OnCount50" Width="60" Height="35" Margin="5,0"
                        x:Name="Count50Btn" Background="#ddd"/>
            </StackPanel>
            
            <!-- 说明 -->
            <Border Background="#fff" CornerRadius="8" Padding="15" Margin="0,10,0,0">
                <StackPanel>
                    <TextBlock Text="难度说明：" FontWeight="Bold" Margin="0,0,0,8"/>
                    <TextBlock Text="• 简单：单字练习，如：一、二、山、水" FontSize="13" Margin="0,0,0,5"/>
                    <TextBlock Text="• 中等：词组练习，如：你好、谢谢、学习" FontSize="13" Margin="0,0,0,5"/>
                    <TextBlock Text="• 困难：句子练习，如：床前明月光" FontSize="13"/>
                </StackPanel>
            </Border>
        </StackPanel>
        
        <!-- 底部 -->
        <TextBlock Grid.Row="2" Text="版本 1.5.0" 
                   FontSize="12" Foreground="Gray"
                   HorizontalAlignment="Center"/>
    </Grid>
</Window>
